# OpenClaw Railway Environment Variables
# Add these in Railway Dashboard > Variables

# Required: Gateway authentication token
# Generate with: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=your_gateway_token_here

# Optional: Setup password for initial configuration via web UI
SETUP_PASSWORD=your_setup_password_here

# Persistent storage paths (use with Railway Volumes)
OPENCLAW_STATE_DIR=/data/.openclaw
OPENCLAW_WORKSPACE_DIR=/data/workspace

# Node.js settings
NODE_ENV=production
NODE_OPTIONS=--max-old-space-size=1536

# Optional: Claude/Anthropic API (if using API-based models)
ANTHROPIC_API_KEY=your_anthropic_api_key

# Optional: Twilio WhatsApp integration
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_WHATSAPP_FROM=whatsapp:+17343367101

# Optional: Telegram Bot integration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Optional: Discord Bot integration
DISCORD_BOT_TOKEN=your_discord_bot_token

# Optional: Slack integration
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your_slack_signing_secret
